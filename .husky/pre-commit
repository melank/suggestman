#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running format, lint, and test checks..."

# フォーマットを実行
npm run format

# リントを実行（エラーがあればコミットを中止）
npm run lint

# テストを実行（エラーがあればコミットを中止）
npm test

echo "✅ All checks passed!"
